// #include <iostream>
// #include <string>
// #include "clvHdMono.hpp"

int
main()
{
    // ClvHd::MonoController clvhd(2);
    // clvhd.start();
    // while(clvhd.nbClients() < 1)
    // {
    //     std::this_thread::sleep_for(std::chrono::milliseconds(100));
    // }

    // std::this_thread::sleep_for(std::chrono::seconds(5));
    // std::cout << "Stop" << std::endl;
    // clvhd.stop();

    return 0;
}